package model;

import javafx.scene.control.Button;


/*This Interface declares that all classes that implement the GameButton interface
 must contain the method createButton
 */
public interface GameButton {

	Button createButton();
	
}
